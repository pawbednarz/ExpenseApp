package com.pbednarz.nonameproject.expense.service;

import com.pbednarz.nonameproject.user.service.ErrorChecker;
import org.springframework.stereotype.Service;

@Service
public class CategoryService implements ErrorChecker {

    // TODO add method, which checks if category is already in database - then theres no need to save it again
}
